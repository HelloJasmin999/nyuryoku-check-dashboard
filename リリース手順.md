# リリース手順（push → 本番URL）

「クロードコード部 Git×Railway上級マスター・速習編 第8回」で実際にやった流れをまとめたもの。次にリリースするとき、これを見れば迷わない。

## 前提（すでにできている）

- GitHubリポジトリに `master`（本番用）と `staging`（検証用）の2つのブランチがある
- Renderに、`master` につながる **本番用Webサービス**（`nyuryoku-check-dashboard-1`）と `staging` につながる **staging用Webサービス**（`nyuryoku-check-dashboard-staging`）の2つがある（それぞれ別のURL、Freeプラン）
- staging用サービスにだけ環境変数 `APP_ENV=staging` を手動で設定してある（Renderの「Environment」タブ）。本番用は未設定のままでOK。
- 無料プランは一定時間アクセスがないとスリープし、次のアクセス時に再起動する（数十秒待つことがある）。また永続ディスクが無いため、サービス再起動のたびに `cases.db` はデモの初期データにリセットされる。

## 日々のリリースの流れ

1. **作業ブランチを作る**（`staging` から分ける）
   ```
   git checkout staging
   git pull origin staging
   git checkout -b 作業ブランチの名前
   ```
2. **コードを直す**
3. **ローカルで動作確認する**
   ```
   python server.py
   ```
   `http://localhost:5000` を開いて、直した部分がちゃんと動くか見る。
4. **コミットする**
   ```
   git add 変更したファイル
   git commit -m "変更の内容が分かる一言"
   ```
5. **GitHubへpushする**
   ```
   git push -u origin 作業ブランチの名前
   ```
6. **PRを作る**：作業ブランチ → `staging`
   - この時点でCI（pytest）が自動実行される（第12回）。ただし`staging`には必須チェック設定が無いので、**赤くてもマージ自体は可能**（自由に試せる練習場）。
7. **PRをマージする**（GitHubの「Merge pull request」→「Confirm merge」）
8. Renderが自動で **staging用サービス** をデプロイする。少し待ってstaging URLを開き、見た目・動きを確認する（スリープしていたら初回アクセスに数十秒かかる）。
9. 問題なければ **PRを作る**：`staging` → `master`
   - `master`には**必須チェック（CI＝pytest）**と**レビュー承認1件必須**が設定されている（第12回）。1人プロジェクトでレビュー承認者がいない場合は、オーナー権限で「Merge without waiting for requirements to be met (bypass rules)」にチェックを入れてマージする。CIチェックは必ず緑になってから行う。
10. **PRをマージする**
11. Renderが自動で **本番用サービス**（master）をデプロイする。本番URLを、PCとスマホ両方で開いて確認する。

つまり「CIが通ったものだけ、自動で本番に出る」というCI/CDの一本のラインが、`staging`（6〜8）→`master`（9〜11）の2段階でつながっている（第13回で確認、2026年8月にRailway→Renderへ移行後も同じ流れを踏襲）。

## 今日のリリースで学んだ注意点

- staging用の表示（例：「検証環境」バッジ）をそのまま`master`にマージすると、**本番にもそのまま出てしまう**。
- 環境ごとに表示や挙動を変えたいときは、環境変数で出し分ける。Railway時代は自動セットされる `RAILWAY_ENVIRONMENT_NAME` を使っていたが、Renderには同等の自動変数が無いため、staging用サービスにだけ手動で `APP_ENV=staging` を設定している（Environmentタブ→Add variable）。
  ```python
  is_staging = os.environ.get("APP_ENV", "") == "staging"
  ```
- Renderの無料プランには永続ディスクが無いので、`cases.db`はサービス再起動のたびにデモ初期データへリセットされる。デモ用途としては「常にきれいな状態に戻る」ので問題ないが、実データを貯め続けたい用途には向かない。

## URL

- 本番: https://nyuryoku-check-dashboard-1.onrender.com
- 検証環境（staging）: https://nyuryoku-check-dashboard-staging.onrender.com

（旧URL・Railway版は2026年8月26日にRenderへ移行済み。Railwayのトライアル終了に伴い無料で使い続けるための切り替え。）
