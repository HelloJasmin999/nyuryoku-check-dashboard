# リリース手順（push → 本番URL）

「クロードコード部 Git×Railway上級マスター・速習編 第8回」で実際にやった流れをまとめたもの。次にリリースするとき、これを見れば迷わない。

## 前提（すでにできている）

- GitHubリポジトリに `master`（本番用）と `staging`（検証用）の2つのブランチがある
- Railwayのプロジェクトに、`master` につながる **production環境** と `staging` につながる **staging環境** の2つがある（それぞれ別のURL）
- Railwayは環境ごとに `RAILWAY_ENVIRONMENT_NAME` という変数を自動でセットしてくれる（productionなら `production`、stagingなら `staging`）。手動設定は不要。

## 日々のリリースの流れ

1. **作業ブランチを作る**（`staging` から分ける）
   ```
   git checkout staging
   git pull origin staging
   git checkout -b 作業ブランチの名前
   ```
2. **コードを直す**
3. **ローカルで動作確認する**
   ```
   python server.py
   ```
   `http://localhost:5000` を開いて、直した部分がちゃんと動くか見る。
4. **コミットする**
   ```
   git add 変更したファイル
   git commit -m "変更の内容が分かる一言"
   ```
5. **GitHubへpushする**
   ```
   git push -u origin 作業ブランチの名前
   ```
6. **PRを作る**：作業ブランチ → `staging`
   - この時点でCI（pytest）が自動実行される（第12回）。ただし`staging`には必須チェック設定が無いので、**赤くてもマージ自体は可能**（自由に試せる練習場）。
7. **PRをマージする**（GitHubの「Merge pull request」→「Confirm merge」）
8. Railwayが自動で **staging環境** をデプロイする。少し待ってstaging URLを開き、見た目・動きを確認する。
9. 問題なければ **PRを作る**：`staging` → `master`
   - `master`には**必須チェック（CI＝pytest）**と**レビュー承認1件必須**が設定されている（第12回）。どちらか片方でも欠けると、マージボタンがグレーアウトして押せない。
10. **PRをマージする**（CIが緑・レビュー承認済みのときだけ可能）
11. Railwayが自動で **production環境**（本番）をデプロイする。本番URLを、PCとスマホ両方で開いて確認する。

つまり「CIが通ったものだけ、自動で本番に出る」というCI/CDの一本のラインが、`staging`（6〜8）→`master`（9〜11）の2段階ですでにつながっている（第13回で確認）。

## 今日のリリースで学んだ注意点

- staging用の表示（例：「検証環境」バッジ）をそのまま`master`にマージすると、**本番にもそのまま出てしまう**。
- 環境ごとに表示や挙動を変えたいときは、Railwayが自動でセットする `RAILWAY_ENVIRONMENT_NAME` を条件分岐に使うと、手動で環境変数を追加登録する手間が省ける。
  ```python
  is_staging = os.environ.get("RAILWAY_ENVIRONMENT_NAME") == "staging"
  ```

## URL

- 本番: https://web-production-3ebcf.up.railway.app
- 検証環境（staging）: https://web-staging-26ef.up.railway.app
