# 開発〜運用の型（Git×Railway上級マスター 修了資料）

「クロードコード部 Git×Railway上級マスター」応用編（第9〜15回）で、このアプリ（nyuryoku-check-dashboard）に積み上げてきた運用の型を1枚にまとめたもの。

## 1. ブランチ運用

- `master` = 本番、`staging` = 検証。作業は必ず`staging`から分けた作業ブランチで行う。
- コミットメッセージは`feat:` `fix:` `docs:`などの型を付ける（第9回）。
- ミスは`git revert`（公開済みの取り消し）／`git reset`（pushする前の手元整理限定）／`git reflog`（resetのやりすぎからの救出）／`git stash`（作業の一時退避）で使い分ける（第10回）。

## 2. レビューとmain保護

- `master`には「CI（pytest）が緑であること」と「レビュー1件承認」の2つの必須条件を設定（第11・12回）。
- 1人開発でレビュアーがいない場合は、オーナー権限の「bypass rules」でマージする（ただしCIは必ず緑になってから）。
- レビュー観点は[docs/レビュー基準.md](./docs/レビュー基準.md)（動くか／漏れがないか／秘密情報がないか／読みやすいか／コミット規約／ドキュメント更新）。

## 3. CI/CD

- push・PRごとにGitHub Actionsでpytestが自動実行される（第12回）。
- `staging`はチェック必須なしの練習場、`master`はCI必須で守られた本番、という2段構え（第13回）。
- CIが通ったものだけ、Renderへ自動デプロイされる一本のライン（詳細は[リリース手順.md](./リリース手順.md)）。

## 4. 環境分離

- `staging`ブランチ→Renderのstaging用サービス、`master`ブランチ→Renderの本番用サービスと、コードとインフラの両方を分離（第13回）。
- 環境ごとの表示・挙動の違いは環境変数（`APP_ENV=staging`）で出し分け、コード自体は共通のまま（第13回でRailwayの自動変数からRenderの手動変数に変更）。
- 本番DBを変更する時は[docs/DB変更チェックリスト.md](./docs/DB変更チェックリスト.md)に沿って、staging→本番の順で確認してから進める（第14回）。

## 5. 独自ドメイン・SSL（第15回）

- 今の本番URLはRenderが自動発行したURL（`nyuryoku-check-dashboard-1.onrender.com`）。SSL（🔒）はこの標準URLの時点ですでに自動で有効になっている。
- 独自ドメインに変える場合は、①ドメインを取得（有料）→②Renderの「Settings → Custom Domains」に追加→③取得元の管理画面でCNAMEレコードを設定→④Renderが自動でSSL証明書を発行、の4ステップ。設定自体は難しくないが、費用が発生するため取得は本人判断とし、今回は手順の理解までとした。

## 6. 監視・ヘルスチェック（第15回）

- UptimeRobotで本番URLを5分おきに監視し、ダウン時にメール通知される設定を追加。
- 無料プランのスリープ復帰pingとしても働くため、副次的に「起こしておく」効果もある。

## 7. 障害復旧

- 気づく（監視）→切り分け（コード or 基盤）→ロールバック（Render即時Redeploy or git revertで正式修正）→復旧確認、の4ステップ。詳細は[docs/障害復旧手順.md](./docs/障害復旧手順.md)。

## 一番効いた回

（ここは提出前にご本人の言葉で追記）
